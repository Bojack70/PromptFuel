{
  "buildCommand": "pnpm --filter @promptfuel/core build && pnpm --filter @promptfuel/web build",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
