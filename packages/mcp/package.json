{
  "name": "@promptfuel/mcp",
  "version": "1.0.2",
  "description": "PromptFuel MCP server — token counting, cost estimation & prompt optimization for Claude Code",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "promptfuel-mcp": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --outDir dist --dts",
    "dev": "tsup src/index.ts --format esm --outDir dist --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@promptfuel/core": "^1.0.3",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  }
}
